<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/main.css" />
    <title>File Uploader</title>
  </head>
  <body class="w-[100vw] h-[100vh] bg-gray-800 grid grid-cols-12 grid-rows-12">
    <%- include("../partials/dashboard-header") %>
    <!--  -->
    <section
      class="col-start-1 col-end-13 row-start-2 row-end-3 bg-gray-900 flex-col gap-2 pl-2"
    >
      <%- include("../partials/dashboard-link", {path: `/dashboard/`, text:
      `home`, iconName: `home`, shrink: false}) %>
      <!--  -->
      <%- include("../partials/dashboard-link", {path:
      `/dashboard/${currentFolder}`, text: `${currentFolder}`, iconName:
      `folder-open`, shrink: false}) %>
    </section>
    <main
      class="col-start-1 col-end-13 row-start-3 row-end-13 bg-gray-800 mx-auto p-4 space-y-4 overflow-y-scroll"
    >
      <form
        action="<%- actionPath %>"
        method="post"
        enctype="multipart/form-data"
        class="w-full md:w-[600px] flex flex-col gap-2"
      >
        <%- include("../partials/section-heading", {text: `Add file to folder
        ${currentFolder}`}) %>
        <!--  -->
        <div class="flex flex-col gap-1">
          <label
            class="block text-gray-200 font-bold font-barlow tracking-wide"
            for="file"
            >select file</label
          >

          <input
            class="bg-gray-800 appearance-none border-2 border-gray-600 rounded-md w-full pr-4 text-gray-200 leading-tight focus:outline-none focus:border-blue-300 cursor-pointer file:bg-blue-300 file:border-0 file:m-0 file:px-2 file:py-1 file:border-r-2 file:border-r-gray-400 file:font-barlow file:text-gray-800 file:font-bold file:tracking-wider file:mr-4"
            type="file"
            name="file"
            id="file"
            accept=".jpg, .jpeg, .png, .svg, .txt, .pdf"
            size="10"
          />
          <p class="text-sm text-gray-400">
            only pdf files with maximum size 1MB
          </p>
        </div>
        <!--  -->
        <%- include("../partials/form-errors", {validationErrors}) %>
        <!--  -->
        <%- include("../partials/form-button", {text: `Upload file`}) %>
      </form>
    </main>
  </body>
</html>
